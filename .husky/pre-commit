#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run full quality gate (format check, type-check, lint check, markdownlint)
bun run quality

# Ensure only affected packages build before committing
bunx turbo run build --filter=...^...
