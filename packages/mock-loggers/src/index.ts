// Mock logger components for development and testing

// Logging
export { MockLogger } from "./MockLogger.js";

// Re-export types for convenience
export type { ILogger, LogLevel, LogContext } from "@network-monitor/shared";
